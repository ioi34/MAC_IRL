% 3장/4장만 단독으로 조판하기 위한 최소 래퍼.
%
% 컴파일:
%   xelatex build-sections-ko && bibtex build-sections-ko \
%     && xelatex build-sections-ko && xelatex build-sections-ko
%
% 한글 조판은 kotex이 있으면 kotex을, 없으면 xeCJK를 사용한다.
% 어느 쪽이든 XeLaTeX 또는 LuaLaTeX으로만 컴파일된다.

\documentclass[sigconf,nonacm]{acmart}

\IfFileExists{kotex.sty}{%
  \usepackage{kotex}%
}{%
  % kotex이 없는 환경(예: 최소 TeX Live)을 위한 대체 경로.
  \usepackage{xeCJK}%
  \IfFontExistsTF{Noto Serif CJK KR}{%
    \setCJKmainfont{Noto Serif CJK KR}%
    \setCJKsansfont{Noto Sans CJK KR}%
    \setCJKmonofont{Noto Sans Mono CJK KR}%
  }{%
    \IfFontExistsTF{Apple SD Gothic Neo}{%
      \setCJKmainfont{Apple SD Gothic Neo}%
    }{%
      \setCJKmainfont{Noto Serif CJK JP}%
    }%
  }%
  \XeTeXlinebreaklocale "ko"%
  \XeTeXlinebreakskip = 0pt plus 0.1em%
}

\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{array}
\usepackage{url}

\settopmatter{printacmref=false,printfolios=true}
\setcopyright{none}

% 정리 환경 이름을 한국어로. acmart가 이미 proposition을 정의하므로 먼저 해제한다.
\makeatletter
\let\proposition\relax \let\endproposition\relax
\makeatother
\theoremstyle{acmplain}
\newtheorem{proposition}{명제}[section]

% 캡션 이름을 본문 표기(그림/표)와 일치시킨다.
\AtBeginDocument{%
  \renewcommand{\figurename}{그림}%
  \renewcommand{\tablename}{표}%
}


\begin{document}

\title{[초안] 방법과 실험 절 조판 확인용}
\author{저자명}
\affiliation{%
  \institution{소속기관}
  \city{서울}
  \country{대한민국}}
\email{author@example.com}
\renewcommand{\shortauthors}{저자명}

\maketitle

\input{sections-method-experiments-ko}

\bibliographystyle{ACM-Reference-Format}
\bibliography{paper-ko,paper-refs-additional}

\end{document}
