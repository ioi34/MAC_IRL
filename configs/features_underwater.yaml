paths:
  processed_dataset: data/processed/dataset_underwater.npz
  processed_metadata: data/processed/metadata_underwater.json

features:
  selected:
    - underwater
    - momentum
    - volatility
    - persist
    - herd_from_foreign
    - herd_from_institution
    - herd_from_retail
  params:
    underwater: {}
    momentum:
      window: 20
    volatility:
      window: 20
      magnitude: false
    persist: {}
    herd_from_foreign: {}
    herd_from_institution: {}
    herd_from_retail: {}

scaling:
  method: standard
