experiment:
  name: final_reward_context
  output_dir: runs/final_reward_context
  save_checkpoints: true

model:
  reward_type: contextual_linear
  context_names:
    - kospi_return_1d
    - fx_level_z_252
