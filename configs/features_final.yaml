paths:
  processed_dataset: data/processed/dataset_final.npz
  processed_metadata: data/processed/metadata_final.json

features:
  selected:
    - underwater
    - herd
    - momentum
    - volatility
  params:
    underwater: {}
    herd:
      window: 5
    momentum:
      window: 20
    volatility:
      window: 20
      magnitude: false

contexts:
  selected:
    - kospi_return_1d
    - fx_level_z_252

scaling:
  method: standard
