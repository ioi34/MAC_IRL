paths:
  processed_dataset: data/processed/dataset_exp_2019_single_herd.npz
  processed_metadata: data/processed/metadata_exp_2019_single_herd.json

features:
  selected:
    - underwater
    - herd
    - momentum
    - volatility
  params:
    underwater: {}
    herd:
      window: 5
    momentum:
      window: 20
    volatility:
      window: 20
      magnitude: false

scaling:
  method: standard
