# GitHub·논문용 그래프 패키지 정리

- 날짜/시간: 2026-07-12 15:11 KST
- 목적: `.gitignore` 대상인 `runs/`의 그래프를 Git 추적 가능한 `experiments/` 폴더로 옮겨 논문과 GitHub에서 사용한다.
- 변경한 것: 연속형 전체 투자자, 바이너리 기준, 기관 E0·E1·E7, 기관 하이퍼파라미터 그래프 17개와 집계 CSV·평가표 8개를 주제별로 복제하고 README와 원본 manifest를 추가했다.
- 고정 조건: 모델, 데이터, 예측값, 평가값과 기존 원본 파일은 변경하지 않았다. 새 학습이나 재평가는 수행하지 않았다.
- 데이터: 기존 CPCV 날짜별 OOS 집계 예측 CSV.
- 설정 파일: 기존 각 실험의 `config_snapshot.yaml`; 이번 작업에서 설정 변경 없음.
- 결과 폴더: `experiments/2026-07-12/paper_figures/`.
- 주요 결과: manifest 25개 파일의 SHA-256이 원본과 모두 일치했고, PNG 17개가 정상 이미지로 확인됐다. 전체 크기는 약 8.3MB이며 `.gitignore` 대상이 아니다.

## 가중치 결과

| 대상 | 피처/가중치 | 값 | 변동성/일관성 | 출처 |
| --- | --- | ---: | ---: | --- |
| 그래프 패키징 | 새 학습 가중치 | 미생성 | 해당 없음 | 기존 실험 결과를 그대로 복제했으며 새 모델을 학습하지 않음 |

- 해석: 수치나 그림 내용의 변경 없이 GitHub 업로드용 경로만 정리했다.
- 다음 액션: `experiments/2026-07-12/paper_figures/README.md`의 권장 순서에 따라 논문 본문과 부록에 배치한다.
