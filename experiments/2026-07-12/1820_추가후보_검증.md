# 추가 후보 검증 (VKOSPI·단기모멘텀·FX레벨) + underwater 제거

- 날짜/시간: 2026-07-12 18:20
- 목적: pooled 4특징 baseline(underwater·momentum·relative·herd) 위에 후보를 하나씩 얹어 out-of-sample 개선분만 채택.
- 변경한 것: 후보 4종 + 결합. 후보열은 원본 CSV에서 계산해 npz 날짜에 정렬(시점 t 상태, 누수 없음).
  - shortmom = 5일 로그수익(단기 모멘텀), 시장공통.
  - fxfeat = fx_level_z_252를 **직접 피처**로(현재는 컨텍스트).
  - vkospictx = vkospi 252일 z-score를 **컨텍스트**로 추가.
  - no_underwater = underwater 제거.
  - best = underwater 제거 + shortmom 추가.
- 고정 조건: κ=1, CPCV 45 split, split별 train-only 스케일링, 투자자별 HP, seed 42. numpy 재현.
- 결과 폴더: `runs/continuous_confirmed/addcand.csv`.
- 주요 결과: **단기모멘텀 채택(개인)·underwater 제거 채택. FX레벨·VKOSPI 기각.** 두 채택은 겹쳐서 누적.

## 후보별 개선분 (pooled base 대비, 방향정확도 / 강도상관)

| 후보 | 외국인 | 개인 | 판정 |
| --- | --- | --- | --- |
| +단기모멘텀(5d) | +0.003 / +0.006 | **+0.012 / +0.008** | 채택(개인) |
| −underwater | +0.002 / **+0.015** | +0.012 / −0.004 | 채택 |
| +FX레벨 직접피처 | **−0.011 / −0.007** | +0.002 / +0.013 | 기각(외국인 방해) |
| +VKOSPI 컨텍스트 | −0.007 / −0.029 | −0.007 / −0.031 | 기각(양쪽 방해) |
| **결합(−underwater+단기모멘텀)** | +0.004 / +0.017 | **+0.020 / +0.002** | 최종 |

## 모델 진행 (외국인 방향/상관 · 개인 방향/상관)

| 단계 | 외국인 | 개인 |
| --- | --- | --- |
| 6특징 미러링 | 0.587 / 0.208 | 0.594 / 0.232 |
| −volatility (5특징) | 0.616 / 0.240 | 0.595 / 0.233 |
| pooled herd (4특징) | 0.624 / 0.253 | 0.599 / 0.243 |
| **최종: −underwater +단기모멘텀 (4특징)** | **0.628 / 0.270** | **0.619 / 0.245** |

## 가중치/판정 요약

| 대상 | 조치 | 근거(개선분) | 출처 |
| --- | --- | --- | --- |
| 전체 | 단기모멘텀(5d) 추가 | 개인 방향 +0.012·상관 +0.008 | addcand.csv |
| 전체 | underwater 제거 | 외국인 상관 +0.015·개인 방향 +0.012 | addcand.csv |
| 전체 | FX레벨 직접피처 기각 | 외국인 −0.011 | addcand.csv |
| 전체 | VKOSPI 컨텍스트 기각 | 양쪽 −0.03 | addcand.csv |

- 해석: in-hand 후보 중 **단기모멘텀만** 값을 함. FX레벨은 이미 컨텍스트 상호작용으로 들어가 직접피처로 넣으면 중복·과적합으로 외국인 방해. VKOSPI(위험회피)는 두 주체 모두 방해 → 일별 강도 설명엔 위험지표가 노이즈. underwater는 전 과정에서 약해 제거가 이득.
- **최종 특징셋: momentum(20d)·relative(20d)·herd(pooled)·shortmom(5d)** (4특징), 컨텍스트 kospi_return_1d·fx_level_z_252. 6특징 미러링 대비 외국인 +0.041 방향/+0.062 상관, 개인 +0.025/+0.013 누적 개선.
- 주의(저장소 반영): volatility 제거·pooled herd·underwater 제거는 config만으로 가능하나, **shortmom은 registry에 단기모멘텀 빌더(예: momentum window=5를 별칭으로) 추가하는 소규모 코드 변경 필요**. numpy 재현으론 검증 완료.
- 다음 액션: (a) shortmom 빌더 추가 후 저장소 스크립트로 최종본 재확인, (b) 남은 검증축(Downstream 예측력, 타종목 재현)로.
