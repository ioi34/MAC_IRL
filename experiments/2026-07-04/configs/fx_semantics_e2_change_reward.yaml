experiment:
  name: fx_semantics_e2_change_reward
  output_dir: runs/fx_semantics_e2_change_reward
  save_checkpoints: true

model:
  reward_type: contextual_linear
  context_names:
    - kospi_return_1d
