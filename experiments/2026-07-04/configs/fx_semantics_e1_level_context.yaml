experiment:
  name: fx_semantics_e1_level_context
  output_dir: runs/fx_semantics_e1_level_context
  save_checkpoints: true

model:
  reward_type: contextual_linear
  context_names:
    - kospi_return_1d
    - fx_level_z_252
