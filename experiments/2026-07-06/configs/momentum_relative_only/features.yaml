paths:
  processed_dataset: data/processed/momentum_relative_only.npz
  processed_metadata: data/processed/momentum_relative_only_metadata.json

features:
  selected:
    - momentum
    - relative
  params:
    momentum:
      window: 20
    relative:
      window: 20

contexts:
  selected:
    - fx_return_1d
    - kospi_return_1d
    - fx_level_z_252
    - vkospi_1d
    - vkospi_return_1d
    - vkospi_return_5d

scaling:
  method: standard
